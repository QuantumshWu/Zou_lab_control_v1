"""Readout-calibration Workbench application package.

Import the explicit ``jobs``, ``window``, or ``app`` leaf that owns the needed
responsibility.  The package root intentionally stays Qt-free.
"""
