\chapter{结论先行}
本报告记录当前仓库中 FPGA pulse-streamer 的 address-switch 入口、Pulse GUI/API 编译边界、server 启动边界和文档同步状态。当前硬件路线是固定 address-switch runtime pulse-streamer：GUI 可以只显示或配置少数 channel，但 server 上传给 FPGA 的 program 始终按 XDC 推断的完整 channel order 补齐，未使用 channel 的 mask bit 为 0。

\begin{figure}[h]
  \centering
  \includegraphics[width=0.98\textwidth]{assets/pulse_streamer_architecture_address_switch.png}
  \caption{控制电脑、FPGA server、Vivado/VIO、FPGA bitstream 和相机外触发的边界。GUI 在前端；full-width 补齐发生在 sequencer compile/upload 边界。}
\end{figure}

本轮重点检查：
\begin{itemize}
  \item \filepath{fpga\build_and_program.bat} 是 build/program/check/diagnose 的统一入口。
  \item \filepath{fpga\run_server.bat} 从 address-switch XDC 推断完整 channel list，默认 fallback 为 \pyapi{ch00...ch61}。
  \item trigger channel 从 XDC label \pyapi{emCCD} 推断，默认是 \pyapi{ch11}。
  \item \filepath{pulses\camera_imaging_address_switch.json} 默认只显示 \pyapi{ch09/ch00/ch03/ch11}，但保存和编译仍保留完整硬件顺序。
\item GUI/API state 即使只包含相机成像子集，也会编译到 full-width program；未配置 channel 全部为 off。
  \item \pyapi{da_dipole/da_bias_y/da_bias_x/da_bias_z} 不再通过普通 TTL mask 展开 ramp；它们使用独立 analog bus segment table。
\end{itemize}

\chapter{入口和目录}
从 repo 根目录运行：

\begin{shellblock}
.\fpga\build_and_program.bat --help
.\fpga\build_and_program.bat --check
.\fpga\build_and_program.bat
.\fpga\run_server.bat --check-config
.\fpga\run_server.bat
\end{shellblock}

默认 Vivado 工程：

\begin{codeblock}[Address-switch build]
fpga\build\address_switch\address_switch.xpr
fpga\build\address_switch\address_switch.runs\impl_1\zlc_pulse_streamer_top_address_switch.bit
fpga\build\address_switch\address_switch.runs\impl_1\zlc_pulse_streamer_top_address_switch.ltx
\end{codeblock}

默认 XDC：

\begin{codeblock}[XDC]
references\source_archives\address_switch\address_switch.srcs\constrs_1\new\addre.xdc
\end{codeblock}

若使用另一份已确认的板级 XDC，设置 \pyapi{ZLC_PS_XDC}。不再需要复制旧 template XDC。

\chapter{编译契约}
Pulse GUI 和 notebook 使用 \pyapi{PulseTableState}。GUI 的 \pyapi{visible_channels} 只影响显示，不影响 FPGA bit order。上传前，sequencer service 会把 state 对齐到 server 的 full channel list：

\begin{codeblock}[Python]
state = na.PulseTableState.load("pulses/camera_imaging_address_switch.json")
program = state.compile(clock_hz=50_000_000, trigger_channels=["ch11"])
\end{codeblock}

默认 camera-imaging preset 的关键通道是：

\begin{codeblock}[Camera subset]
ch09 trap
ch00 cooling
ch03 probe
ch11 emCCD
\end{codeblock}

默认 \pyapi{camera_exposure_ns=19980000} 时，50 MHz 编译结果为：

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\textwidth]{assets/camera_imaging_address_switch_edges.png}
  \caption{camera\_imaging\_address\_switch preset 的 edge table。图中只使用相机成像子集，上传 program 仍然带完整 channel list。}
\end{figure}

\begin{codeblock}[camera imaging address-switch]
ticks: 0, 100000, 105000, 106000, 1105000, 1106000
masks: 513, 512, 2568, 520, 512, 0
trigger_count: 1
repeat_forever: true
\end{codeblock}

\pyapi{513 = ch00 + ch09}，\pyapi{2568 = ch03 + ch09 + ch11}。这说明相机外触发 bit 是 \pyapi{ch11/emCCD}，不是 \pyapi{ch03}。

\chapter{scan 和容量}
scan 使用一个 named scan table file，并由 host 打包到当前两轴 timing scan RAM。单个 FPGA program 使用有限 scan rows；大 scan file 可以由 API 分 chunk 连续上传运行：

\begin{codeblock}[Scan table]
# vars: camera_exposure_ns(ns), trig_delay(ns)
1000 0
2000 20
4000 40
\end{codeblock}

所有 timing 值单位是 ns，默认 50 MHz 硬件下必须对齐到 20 ns。FPGA 上传的是 edge template 加 packed scan RAM，不会把 scan row 展开成大量 period column。

build/check 使用资源目标参数作为 sizing guide：

\begin{shellblock}
$env:ZLC_PS_RESOURCE_TARGET_PCT = "70"
$env:ZLC_PS_MAX_EDGES = "512"
$env:ZLC_PS_MAX_SCAN_POINTS = "256"
.\fpga\build_and_program.bat --check
\end{shellblock}

Vivado \pyapi{report_utilization} 和 \pyapi{report_timing_summary} 是最终证据；Python/Tcl 的容量打印是 build 前的预算提示。

\chapter{analog bus segment 验证}
address-switch bitstream 仍然保留 62-bit digital \pyapi{prog_mask}，但 DA 类 10-bit bus 现在走独立 segment upload。普通 TTL 和 DA bus 是两张表：

\begin{figure}[h]
  \centering
  \includegraphics[width=0.96\textwidth]{assets/edge_vs_bus_segment_table.png}
  \caption{普通 edge table 与 analog bus segment table 的执行角色不同。}
\end{figure}

\begin{codeblock}[Bus segment row]
bus_id, start_tick, stop_tick, start_value, stop_value, mode
\end{codeblock}

\pyapi{edge} segment 的 \pyapi{start_tick == stop_tick}，运行到该 tick 时直接更新 bus value。\pyapi{ramp} segment 使用 \pyapi{start_tick/stop_tick} 和 \pyapi{start_value/stop_value}，由 FPGA 内部 DDA accumulator 在每个 clock tick 计算当前 stair-step。这样一个长 ramp 只占一个 bus segment，不会把普通 \pyapi{tick/mask} edge table 填满。

\begin{figure}[h]
  \centering
  \includegraphics[width=0.96\textwidth]{assets/analog_bus_segment_flow.png}
  \caption{从 GUI/API 到 Verilog runtime 的 bus segment 上传和执行路径。}
\end{figure}

默认 VIO output probe 从 21 个扩展到 31 个：

\begin{codeblock}[Bus VIO probes]
probe_out21 zlc_bus_prog_we          width 1
probe_out22 zlc_bus_prog_bus         width 2
probe_out23 zlc_bus_prog_addr        width 6
probe_out24 zlc_bus_prog_start_tick  width 32
probe_out25 zlc_bus_prog_stop_tick   width 32
probe_out26 zlc_bus_prog_start_value width 10
probe_out27 zlc_bus_prog_stop_value  width 10
probe_out28 zlc_bus_prog_mode        width 2
probe_out29 zlc_bus_counts           width 28
probe_out30 zlc_scan_prog_bus_values width 40
\end{codeblock}

四个 bus 的硬件顺序为 \pyapi{da_dipole, da_bias_y, da_bias_x, da_bias_z}，每个 bus 最多 64 个 segment。没有使用的 bus 在 \pyapi{bus_counts} 中 count 为 0，因此旧 RAM 内容不会被执行。普通 TTL 输出继续使用 \pyapi{tick/mask} edge rows；一个长 DA ramp 只消耗一个 bus segment，不消耗几百个 ordinary edge rows。静态 edge-mode DA value scan 通过 \pyapi{zlc_scan_prog_bus_values} 每个 scan row 上传一个 packed 4x10-bit 值；当前限制是 hardware scan array 不能和 analog bus ramp segment 同一次上传，如果需要扫描 analog ramp，应按 scan point 逐个 prepare/fire。

\chapter{验证摘要}
当前交付需要覆盖的最小检查：

\begin{itemize}
  \item \pyapi{python -m py_compile} 覆盖 sequencer、server、frontend GUI 和 fpga backend。
  \item \pyapi{python -m json.tool} 覆盖三份 tutorial notebook。
  \item \filepath{fpga\build_and_program.bat --help} 和 \filepath{fpga\run_server.bat --help} 显示 address-switch 默认路径、50 MHz clock 和 XDC-inferred channel list。
  \item \filepath{pulses\camera_imaging_address_switch.json} 可编译出上述 ticks/masks。
\end{itemize}

\chapter{上机顺序}
\begin{enumerate}
  \item 在 Verilog/FPGA 电脑把 repo 放到短路径，例如 \filepath{D:\ZLC}。
  \item 运行 \filepath{install_requirements.bat}。
  \item 设置 \pyapi{ZLC_PS_VIVADO_BIN}，或确认 Vivado 自动发现路径正确。
  \item 运行 \filepath{fpga\build_and_program.bat --check}。
  \item 运行 \filepath{fpga\build_and_program.bat}，确认 \filepath{.bit/.ltx} 生成并 program 成功。
  \item 运行 \filepath{fpga\run_server.bat --check-config}，确认 project、bit、ltx、XDC、channel、trigger、clock 都正确。
  \item 运行 \filepath{fpga\run_server.bat} 保持 server 窗口打开。
  \item 在 FPGA 电脑或控制电脑运行 \filepath{pulse_gui.bat --remote-host ... --state .\pulses\camera_imaging_address_switch.json}。
  \item 按 \pyapi{On Pulse} 前确认示波器接到 \pyapi{ch09/ch00/ch03/ch11} 对应的物理输出。
\end{enumerate}
