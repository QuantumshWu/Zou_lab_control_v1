\chapter{结论先行}
本报告记录当前仓库中 FPGA pulse-streamer 的 40-channel 入口、Pulse GUI/API 编译边界、server 启动边界和文档同步状态。当前硬件路线是固定的 40 路 runtime pulse-streamer：GUI 可以只显示或配置少数 channel，但 server 上传给 FPGA 的 program 始终按 \pyapi{ch00...ch39} 补齐，未使用 channel 的 mask bit 为 0。

\begin{figure}[h]
  \centering
  \includegraphics[width=0.98\textwidth]{assets/pulse_streamer_architecture.png}
  \caption{控制电脑、FPGA server、Vivado/VIO、FPGA bitstream 和 qCMOS trigger 的边界。GUI 在前端；40 路补齐发生在 sequencer compile/upload 边界。}
\end{figure}

本轮重点检查：
\begin{itemize}
  \item \filepath{fpga\build_and_program.bat} 是 40 路 build/program/check/diagnose 的统一入口。
  \item \filepath{fpga\run_server.bat} 永远启动 \pyapi{ch00...ch39} 的 40 路 server，trigger 为 \pyapi{ch03}。
  \item \filepath{fpga\pulse_streamer} 下旧的分散 Windows bat 已删除，Vivado build 产物目录已从源码树清理。
  \item \filepath{pulses\camera_imaging_40ch.json} 默认只显示前四路，但保存和编译仍保留完整 40 路硬件顺序。
  \item 只包含 \pyapi{ch00/ch03} 的 GUI/API state 编译到 40 路硬件时，\pyapi{program.channels} 是完整 40 路，所有高于 \pyapi{ch03} 的 mask bit 都为 0。
\end{itemize}

\chapter{入口和目录}
从 repo 根目录运行：

\begin{shellblock}
.\fpga\build_and_program.bat --help
.\fpga\build_and_program.bat --check
.\fpga\build_and_program.bat
.\fpga\run_server.bat
\end{shellblock}

\pyapi{--check} 做无物理 pin XDC 的 40 路 synth 自查；真实 build/program 默认使用已经从旧 \filepath{address_switch} pin map 生成的 \filepath{fpga\pulse_streamer\zlc_pulse_streamer_40ch.xdc}。如果板卡或转接线不同，设置 \pyapi{ZLC_PS_40CH_XDC} 指向新的板级 XDC。脚本会拒绝仍包含 \pyapi{<PIN_CHxx>} placeholder 的 XDC。

\filepath{pulse_gui.bat} 仍在 repo 根目录，因为它是前端入口，不是 FPGA build/server 脚本：

\begin{shellblock}
.\pulse_gui.bat --remote-host 127.0.0.1 --remote-port 18861 --state .\pulses\camera_imaging_40ch.json
\end{shellblock}

\chapter{40-channel 编译契约}
\pyapi{PulseTableState} 可以包含完整 40 路，也可以只包含当前使用的硬件 channel 子集。真正上传前，\pyapi{compile_pulse_table_runtime_program(state, channels=hardware_channels)} 使用 server 的完整硬件 channel list 作为 bit order：

\begin{codeblock}[Python]
hardware_channels = [f"ch{i:02d}" for i in range(40)]
state = na.PulseTableState(
    channels=["ch00", "ch03"],
    periods=[
        na.PulsePeriod(10, (1, 0), unit="ns"),
        na.PulsePeriod(10, (0, 1), unit="ns"),
        na.PulsePeriod(10, (0, 0), unit="ns"),
    ],
    visible_channels=["ch00", "ch03"],
)
program = na.compile_pulse_table_runtime_program(
    state,
    channels=hardware_channels,
    clock_hz=100_000_000,
    trigger_channels=["ch03"],
)
\end{codeblock}

预期结果：

\begin{codeblock}[Expected]
program.channels == ["ch00", ..., "ch39"]
program.masks == [1 << 0, 1 << 3, 0, 0]
all(mask >> 4 == 0 for mask in program.masks)
program.repeat_forever is True
\end{codeblock}

这就是“GUI 只显示少数 channel，FPGA 仍然正常跑 40 路”的边界。Verilog 不需要知道 GUI 隐藏了哪些行；它只接收已经补齐的 40-bit mask。

\chapter{Camera Imaging Preset}
\filepath{pulses\camera_imaging_40ch.json} 是当前相机成像 preset。它的硬件 channel 是 \pyapi{ch00...ch39}，默认 visible channel 是 \pyapi{ch00/ch01/ch02/ch03}，display label 分别是 \pyapi{trap/cooling/probe/qcm_trigger}。\pyapi{camera_exposure} period 使用 \pyapi{duration="x"} 和默认 \pyapi{x_ns=19980000}，所以控制电脑 notebook 中的 \pyapi{pulse.x = ...} 会直接改变 probe/readout exposure；默认 \pyapi{x_ns} 下的 edge table 仍是：

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\textwidth]{assets/camera_imaging_40ch_edges.png}
  \caption{40-channel camera imaging preset 的 edge table。图中只使用前四路，上传 program 仍然带完整 40 路 channel list。}
\end{figure}

100 MHz 下这个 preset 编译为：

\begin{codeblock}[camera imaging 40ch]
ticks: 0, 200000, 210000, 212000, 2210000, 2212000
masks: 3, 1, 13, 5, 1, 0
trigger_count: 1
repeat_forever: true
\end{codeblock}

\chapter{验证命令}
本轮相关验证命令：

\begin{shellblock}
cmd /c fpga\build_and_program.bat --help
cmd /c fpga\run_server.bat --help
cmd /c fpga\build_and_program.bat --check
cmd /c "set ZLC_NO_PAUSE=1&&fpga\build_and_program.bat --help"
cmd /c fpga\run_server.bat --check-config
cmd /c fpga\build_and_program.bat --diagnose
python -m json.tool tutorials\neutral_atom_fpga_server.ipynb
python -m json.tool tutorials\neutral_atom_hardware_quickstart.ipynb
pytest -q tests\test_neutral_atom_lightweight.py -k "repo_vivado_entrypoint_contract or checked_in_camera_imaging_pulse or differential_edge_upload"
pytest -q tests\test_frontend_smoke.py -k "render_tex_pdf or pulse_gui"
\end{shellblock}

\section{Vivado 40ch 路径和 build 结果}
当前脚本的 Vivado 工程 source of truth 是 \filepath{fpga\build_and_program.bat} 打印的项目目录。默认 build root 是 repo 内的 \filepath{fpga\build}，真实 40 路工程是 \filepath{fpga\build\p40}，工程名是 \filepath{p40}。不要再把旧 \filepath{fpga\pulse_streamer\build} 里的工程、bitstream 或 probes 当作当前工程源。

当前无耗时重跑 build 的脚本级验证结果如下：

\begin{codeblock}[Vivado build-only summary]
Vivado: C:\Xilinx\Vivado\2019.1\bin\vivado.bat
ZLC build root: <repo>\fpga\build
Project dir: <repo>\fpga\build\p40
Top: zlc_pulse_streamer_top_40ch
CHANNEL_COUNT: 40
EDGE_ADDR_WIDTH: 10
MAX_EDGES: 1024
Expected bitstream: zlc_pulse_streamer_top_40ch.bit
Expected probes: zlc_pulse_streamer_top_40ch.ltx
\end{codeblock}

build 完成后产物应在脚本打印的短路径工程目录中：

\begin{shellblock}
<repo>\fpga\build\p40\p40.runs\impl_1\zlc_pulse_streamer_top_40ch.bit
<repo>\fpga\build\p40\p40.runs\impl_1\zlc_pulse_streamer_top_40ch.ltx
\end{shellblock}

\filepath{fpga\run_server.bat --check-config} 会先检查上述 \filepath{.ltx}。如果 \filepath{fpga\build\p40} 还没有 build 产物，它会明确报错并要求先运行 \filepath{fpga\build_and_program.bat} 或手动设置 \pyapi{ZLC_PS_VIVADO_LTX}；这比误用旧 \filepath{fpga\pulse_streamer\build} 里的 \filepath{.ltx} 更安全。build 成功后，server channel list 应为完整 \pyapi{ch00...ch39}，trigger 为 \pyapi{ch03}。

\section{硬件 target 诊断状态}
\filepath{fpga\build_and_program.bat --diagnose} 是非破坏性硬件枚举，不会 program FPGA。当前机器上该诊断返回：

\begin{codeblock}[diagnose result]
No hardware targets found.
Check USB/JTAG cable, board power, cable drivers, and hw_server.
\end{codeblock}

因此本报告证明到“40ch bitstream 可以完整生成，server 会指向正确 bit/LTX”的层级；真正 program FPGA、启动 server、从 GUI/API 发 \pyapi{On Pulse} 并在示波器上看输出，仍需要在能枚举到 JTAG target 的 Verilog/FPGA 电脑上执行。

如果在真实 FPGA 电脑上继续验证，推荐顺序是：

\begin{enumerate}
  \item 把 repo 放到短路径，例如 \filepath{D:\ZLC}。
  \item 设置 \pyapi{ZLC_PS_VIVADO_BIN}，如果 Vivado 不在默认搜索路径。
  \item 核对 \filepath{zlc_pulse_streamer_40ch.xdc} 的 40 路 pin map；换板子时设置 \pyapi{ZLC_PS_40CH_XDC}。
  \item 运行 \filepath{fpga\build_and_program.bat --check}。
  \item 运行 \filepath{fpga\build_and_program.bat} 生成并 program 40 路 bitstream。
  \item 运行 \filepath{fpga\run_server.bat}。
  \item 从 FPGA 电脑或控制电脑运行 \filepath{pulse_gui.bat --remote-host ... --state .\pulses\camera_imaging_40ch.json}。
\end{enumerate}
