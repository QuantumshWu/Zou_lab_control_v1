"""Hamamatsu DCAM Python wrapper shipped with the qCMOS device adapter.

The modules in this package are vendor driver bindings.  They are kept out of
the main device adapter layer so experiment code imports ``QCMOSCamera`` rather
than depending on top-level ``dcam`` modules being installed in the kernel.
"""

